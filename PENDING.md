# کارهای باقی‌مانده — شناسایی‌شده در این سِشن ولی انجام‌نشده

> فقط فهرست است؛ انجام نشده‌اند. وضعیت بقیه‌ی کارها: `README.md` و ریلیز [v1.1.1](https://github.com/amirlwf/anjam/releases/tag/v1.1.1).
> (نسخه‌ی v1.1.1: زنگ دقیق کارها + پومودورو + ریسپانسیو عمیق اندروید — با QA ۴۵/۴۵ روی بیلد بسته‌بندی‌شده پاس شده؛ نسخه‌های قبل: v1.1.0 تایمر/تقویم/فونت، v1.0.0 نسخه اولیه.)

## ۱. تست end-to-end همگام‌سازی با Supabase واقعی ⛔ مهم‌ترین
- **چرا انجام نشده:** Project URL و anon key پروژه‌ی Supabase هنوز داده نشده (گفته شد «بعداً می‌دهم»).
- **چه باید کرد:**
  1. اجرای `supabase/schema.sql` در SQL Editor پروژه (بلوک realtime آن هم — `schema.sql:73`).
  2. خاموش کردن «Confirm email» در Authentication → Providers → Email.
  3. وارد کردن URL + anon key در ⚙ تنظیمات اپ → Connect → ثبت‌نام با یک ایمیل.
  4. ساخت یک تسک روی ویندوز و دیدن آن روی اندروید (و بالعکس)، بدون رفرش دستی.

## ۲. نصب و تست APK روی گوشی واقعی (به‌خصوص آلارم)
- APK امضا و `apksigner verify` شده، کلاس‌های AlarmActivity/AlarmReceiver/AlarmScheduler/AlarmPlugin و پرمیشن‌های SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM / USE_FULL_SCREEN_INTENT / VIBRATE داخل dex تأیید شده — ولی روی هیچ دستگاهی نصب نشده.
- باید روی گوشی واقعی چک شود: زنگ تایمر و **زنگ دقیق کارها** (ساعت+روز تعیین‌شده) با صفحه خاموش و قفل، دکمه‌ی «توقف/۵ دقیقه بعد»، لرزش، چرخه‌ی پومودورو، آیکون لانچر جدید، اسپلش، چرخش صفحه، وضعیت نوار (status bar هماهنگ با تم)، کیبورد هنگام تایپ در اینپوت‌ها، و رفتار Android 13+ با پرمیشن exact alarm (در صورت نیاز Settings → Alarms & reminders).

## ۳. نصب واقعی `Anjam-Setup-1.1.0.exe` روی ویندوز
- نصب‌کننده ساخته و امضاشده ولی یک‌بار هم نصب نشده.
- باید چک شود: آیکون دسکتاپ/منوی استارت با لوگو، اجرا از مسیر Program Files، حذف نصب (Uninstall).

## ۴. بازخورد دکمه‌ی «Test connection» (باگ احتمالی)
- در تست با credential جعلی، بعد از کلیک روی Test هیچ پیام خطا/موفقیتی روی صفحه دیده نشد (`release/qa/02-error-path.png`).
- احتمال: پیام نمایش داده نمی‌شود یا قبل از خواندن ناپدید شده — باید بررسی و در صورت نیاز اصلاح شود.

## ۵. تست حالت آفلاین و حل تعارض
- رفتار بدون اینترنت (صف نوشتن‌ها و ارسال بعد از وصل شدن) و سناریوی ویرایش همزمان یک تسک روی دو دستگاه تست نشده.
- (ورود جعلی آفلاین + رندر فهرست/افزودن تسک در QA پاس شده؛ صف همگام‌سازی واقعی تست نشده.)

## ۶. ریوک کلید GitHub
- کلید `ghp_…` که برای پوش استفاده شد — طبق گفته باید ریوک شود و هنوز ریوک نشده.
- (از این پس پوش با کلید جدید یا SSH انجام شود؛ کلید در تاریخچه‌ی این سِشن است.)

## ۷. تمیزکاری کوچک
- `scripts/cdp-ux-check.mjs` سید جعلی دارد (storageKey برابر `anjam.auth`) — برای QA بدون Supabase؛ اگر خواستی سید واقعی جایگزین شود.
- خروجی‌های QA در `release/qa/*.png` و apk در `releases/` کامیت می‌شوند؛ در صورت سنگین شدن repo به `.gitignore` اضافه شود.
